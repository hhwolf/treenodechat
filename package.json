{
  "name": "threadline-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 4174",
    "start": "python3 -m http.server 4174",
    "test": "node --check app.js && node --check pretext-layout.js && node --test"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
